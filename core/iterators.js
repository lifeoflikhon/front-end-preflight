// for(const number = 0; number < 10; number++) {
//   //console.log(number);
// }

// const number = 0;
// while (number < 10) {
//   //console.log(number);
//   number++;
// }

// const number = 0;
// do {
//   //console.log(number);
//   number++;
// } while (number < 10);


// ==================
const name = '-shi-fat-';
const letters = [];
for(let i = 0; i < name.length; i++) {
  letters.push(name[i]);
}
console.log(letters);

// forEach
// names.forEach((name) => console.log(name));

// Map
// console.log(names.map((name) => 'md. ' + name));

// Filter
// console.log(names.filter((name) => name === 'mehedi'));

// Find
// console.log(names.find((name) => name === 'mehedi'));

// Reduce
// console.log(numbers.reduce((total, curr) => total + curr, 0));

// Splice
// console.log(numbers.splice(1, 2));

// Split
// console.log(name.split(''));